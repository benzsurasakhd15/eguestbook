const crypto=require('crypto');const {session}=require('./_wanwan');
module.exports=(req,res)=>{if(req.method!=='POST')return res.status(405).end();const body=typeof req.body==='string'?JSON.parse(req.body||'{}'):req.body||{};if(body.pin!==process.env.WANWAN_CLOUD_PIN)return res.status(401).json({error:'PIN ไม่ถูกต้อง'});const d=new Date();const event=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}_${crypto.randomBytes(4).toString('hex').toUpperCase()}`;const token=session({event,exp:Date.now()+12*60*60*1000});res.setHeader('Set-Cookie',`wanwan_upload_session=${token}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=43200`);res.status(200).json({event});};
