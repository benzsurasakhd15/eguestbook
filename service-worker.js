self.addEventListener('install',event=>event.waitUntil(caches.open('eguestbook-v3').then(cache=>cache.addAll(['/','/index.html','/manifest.webmanifest','/icon.svg']))));self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!=='eguestbook-v3').map(key=>caches.delete(key))))));self.addEventListener('fetch',event=>event.respondWith(caches.match(event.request).then(hit=>hit||fetch(event.request))));
